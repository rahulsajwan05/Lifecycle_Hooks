import { PercentagepipePipe } from './percentagepipe.pipe';

describe('PercentagepipePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentagepipePipe();
    expect(pipe).toBeTruthy();
  });
});
